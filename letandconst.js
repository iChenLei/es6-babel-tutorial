‘use strict’; //严格模式
{
    let a = 10;
    var b = 1;
}

// babel transelate let to var

let //不存在变量提升
    //先声明后使用

typeof  //会出现错误

let //在作用域内不能多次声明变量

const //只读变量，不能修改 babel transelate it to var (right now)


